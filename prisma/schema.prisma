generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Video {
  id             String @id
  publishedAt    String
  title          String
  thumbnails     String
  genres         String
  viewCount      String
}
